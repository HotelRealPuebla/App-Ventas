/*
import 'package:flutter/material.dart';

class InfoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: const Text('Acerca de '), backgroundColor: Colors.blueAccent),
      body: Container(
        padding: const EdgeInsets.all(15),
        child: Column(
          children: const <Widget>[
            Text('Acerca de '),
            Divider(height: 10),
            Card(
              child: ListTile(
                title: Text('Universidad Tecnológica de'),
                subtitle: Text(' Xicotepec de Juárez, Puebla'),
              ),
            ),
            Card(
              child: ListTile(
                  title: Text('Sabdiel Gutiérrez Mejía'),
                  subtitle: Text('ING. en Gestión y  Desarrollo de Software')),
            ),
          ],
        ),
      ),
    );
  }
}
*/